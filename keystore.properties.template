# Juno Release Keystore configuration template
# Copy this file to keystore.properties in the same directory and fill in the actual values.
# Do NOT commit keystore.properties to version control.
storeFile=app/keystore/juno-release.keystore
keyAlias=juno
storePassword=your_keystore_password_here
keyPassword=your_key_password_here
